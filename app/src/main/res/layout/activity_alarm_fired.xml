<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#050F0E">

    <!-- Compose sky background -->
    <androidx.compose.ui.platform.ComposeView
        android:id="@+id/composeBackground"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- Content overlay -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:orientation="vertical"
        android:padding="32dp">

        <!-- Pulsing glow ring around time -->
        <FrameLayout
            android:layout_width="220dp"
            android:layout_height="220dp"
            android:layout_gravity="center">

            <!-- Outer glow ring -->
            <View
                android:id="@+id/glowRing"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="@drawable/alarm_glow_ring" />

            <!-- Time display centered -->
            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:gravity="center"
                android:orientation="vertical">

                <LinearLayout
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:baselineAligned="true"
                    android:gravity="bottom"
                    android:orientation="horizontal">

                    <TextView
                        android:id="@+id/tvAlarmTime"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="5:30"
                        android:textColor="#FFFFFF"
                        android:textSize="56sp"
                        android:textStyle="bold"
                        android:fontFamily="sans-serif-light"
                        android:letterSpacing="-0.02" />

                    <TextView
                        android:id="@+id/tvAmPm"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_marginStart="8dp"
                        android:text="AM"
                        android:textColor="#1C9C91"
                        android:textSize="20sp"
                        android:textStyle="bold" />
                </LinearLayout>

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="4dp"
                    android:text="Vérité Alarm"
                    android:textColor="#80B4D2CD"
                    android:textSize="14sp"
                    android:letterSpacing="0.15" />
            </LinearLayout>
        </FrameLayout>

        <View
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_weight="1" />

        <!-- Snooze Button -->
        <TextView
            android:id="@+id/btnSnooze"
            android:layout_width="280dp"
            android:layout_height="56dp"
            android:layout_marginBottom="16dp"
            android:background="@drawable/btn_outline_teal"
            android:gravity="center"
            android:text="Snooze  ·  5 min"
            android:textColor="#1C9C91"
            android:textSize="16sp"
            android:textStyle="bold"
            android:letterSpacing="0.03" />

        <!-- Dismiss Button -->
        <TextView
            android:id="@+id/btnDismiss"
            android:layout_width="280dp"
            android:layout_height="56dp"
            android:layout_marginBottom="48dp"
            android:background="@drawable/pill_bg"
            android:backgroundTint="#1C9C91"
            android:gravity="center"
            android:text="Dismiss"
            android:textColor="#FFFFFF"
            android:textSize="16sp"
            android:textStyle="bold"
            android:letterSpacing="0.03" />

    </LinearLayout>
</FrameLayout>
