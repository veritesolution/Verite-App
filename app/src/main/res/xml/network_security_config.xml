<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!--
        Development: allow cleartext HTTP for local servers (emulator + LAN).
        Production builds should switch to HTTPS and set cleartextTrafficPermitted="false".
    -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Debug builds: also trust user-installed CA certificates -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
